syntax = "proto3";

message ChatRequest {
	string text = 1;
}

message ChatResponse {
	string text = 1;
	string image_path = 2;
}

service BotVectorDB {
	rpc Query (ChatRequest) returns (ChatResponse);
}
